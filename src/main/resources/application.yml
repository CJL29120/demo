server:
  port: 9000

spring:
  application:
    name: demo
  profiles:
    include: db


  datasource:
    driver-class-name:
    url:
    username:
    password:

mybatis-plus:
  mapper-locations: classpath:/mapper/*.xml
#  configuration:
#    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl


logging:
  level:
    root: info
  config: classpath:logback-spring-dev.xml
common:
  logDir: d:\logs\${spring.application.name}
